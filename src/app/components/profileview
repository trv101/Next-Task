/* "use client"; 
import { useState } from "react";

export default function ProfViewer() {
  // ✅ List of Random Faces
  const faces = [
    "/viewer1.jpg",
    "/viewer2.jpg"
  ];

 
  const [randomFace, setRandomFace] = useState("");


  // ✅ State for the Bio
  const [bio, setBio] = useState("");

  function handleBioChange(e) {
    setBio(e.target.value);
  }

  return (
    <div className="prof-viewer">
     
      <img src={faces} alt="Random Face" className="viewer" />

     
      <div className="bio-section">
        <h3>Bio:</h3>
        <p>{bio}</p>
      </div>

    
      <textarea
        placeholder="Write your bio here..."
        value={bio} onChange ={handleBioChange}
        className="bio-textarea"
      ></textarea>
    </div>
  );
}

*/